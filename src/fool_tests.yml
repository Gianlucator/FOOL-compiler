due classi:
- class A implements C (int a) {

      int f(int x) 2 * 2;
      int r(int y) 2 * y;
  }
  class B implements A (int a) {

      int f(int x) 2 * 2;
      int r(int y) 2 * y;
  }
  class C implements B (int a) {

      int f(int x) 2 * 2;
      int r(int y) 2 * y;
  };

  let int x = 2;
  in 2 * x;
- ""