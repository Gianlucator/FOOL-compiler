classi circolari:
- class A implements C (int a) {

      int f(int x) 2 * 2;
      int r(int y) 2 * y;
  }
  class B implements A (int a) {

      int f(int x) 2 * 2;
      int r(int y) 2 * y;
  }
  class C implements B (int a) {

      int f(int x) 2 * 2;
      int r(int y) 2 * y;
  };

  let int x = 2;
  in 2 * x;
- ""

classi normali:
- class B implements A (int a) {

      int f(int x) 2 * 2;
      int r(int y) 2 * y;
  }
  class A(int a) {

      int f(int x) 2 * 2;
      int r(int y) 2 * y;
  };
  let int x = 2;
  in 2 * x;
- ""

classi inheritance:
- class Sovrano(int a) {

     int f(int x) 2 * 2;
     int r(int y) 2 * y;
 }
 class Automa implements Sovrano (int a) {

     int f(int x) 2 * 2;
     int r(int y) 3 * y;
 }
 class S(int a) {

     int f(int x) 2 * 2;
     int r(int y) 2 * y;
 }
 class A implements S (int a) {

     int f(int x) 2 * 2;
 };
 let Sovrano s = new Sovrano(4); in t.f();
- ""

senza classi:
- let int x = 4; int f(int y) x + y; in f(x);
- ""