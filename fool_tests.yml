#senza classi:
#- let int x = 4; int f(int y) x + y; int g(int z) x + z;
#  in print(g(f(4)));
#- "8"

classe singola dec:
- class Rectangle(int l1, int l2) {
    int area() l1 * l2;
  };

  let Rectangle r = new Rectangle(2, 4);
  in print(r.area());
- "8"

inheritance semplice:
- class A {
    int u() 1;
  }
  class B implements A {
    int s(int x) x + 1;
  };

  let A x = new B;
  in print(x.u());
- "1"

overriding:
- class A {
    int u() 1;
  }
  class B implements A {
    bool u() true;
    int s(int x) x + 1;
  };

  let B x = new B;
  in print(x.u());
- "true"



